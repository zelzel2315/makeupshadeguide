<br><%= link_to 'Add new makeup', makeups_path %>
<br>
<br>
<h3> Your true shade is </h3>
<%= @user.makeups.first.true_shade.name %>
<br><%= @user.makeups.first.true_shade.description %>
<br><%= link_to 'Find other makeups in your True Shade', makeups_path %>
<br>
<br>

<div class="makeupimages">
  <h3> This is your collection list </h3>
  <% @user.makeups.each do |m| %>
  <%= m.brand %> <br>
  <%= m.product %> <br>
  <%= m.shade %> <br>
  <%= image_tag(m.image) %>
  <% end %>
  <br>
  <br>
</div>
